# Ragas and LangChain versions depend on each other; this combination is tested.
# Newer releases (ragas 0.4.x) expect langchain-community modules that were
# removed, and fail on import.
ragas==0.2.14
langchain-community==0.3.14
langchain-openai==0.2.14

datasets
httpx
numpy
openai
python-dotenv
requests

# Web UI
fastapi
uvicorn

# Use the Windows certificate store on networks that inspect TLS.
pip-system-certs
certifi
